# MedShare 8 次实验报告分工（仅写已实现内容 + 每次可一键验证）

## 0. 使用规则

- 只写仓库里已经落地的功能，不写"计划中但未实现"内容。
- 环境配置章节已完成，本文件不再分配。
- 每次报告 5 个人写 5 个不同小节，不能重复。
- 每次都附一条可直接执行的验证命令（PowerShell）。

## 1. 成员映射（先替换）

- 成员 A：侯懿
- 成员 B：张田欣
- 成员 C：乔鹏博
- 成员 D：吴昶之
- 成员 E：何欣航

## 2. 一次性准备（只做一次）

- 后端依赖：`cd backend; pip install -r requirements-dev.txt`
- 链码依赖：`cd ..\fabric-network\chaincode\medshare\javascript; npm install`

## 3. 八次报告分工（全部对应已实现功能）

### 第 1 次：认证安全 + 测试基线

已实现内容范围：
- bcrypt 密码哈希、JWT、.env 配置化。
- 注册、登录、改密、whoami。
- 链码单测框架（Mocha/Chai/Sinon）已建。

5 人分工：
- 成员 A：实验目标与安全问题背景（明文密码到哈希）。
- 成员 B：后端认证接口实现与数据流。
- 成员 C：数据库与配置改造点（models/config/init.sql）。
- 成员 D：链码测试框架与测试组织方式。
- 成员 E：`test_auth.py` 与链码测试结果统计。

一键验证命令：
`cd backend; pytest tests/test_auth.py -v; cd ..\fabric-network\chaincode\medshare\javascript; npm test`

### 第 2 次：病历版本链与修订追溯

已实现内容范围：
- 版本键与 `LATEST` 热点键。
- revise/history 相关后端接口。
- 前端版本标签与版本链展示。

5 人分工：
- 成员 A：链码版本数据结构（version、previousTxId）。
- 成员 B：后端 revise/history 接口。
- 成员 C：前端版本链交互。
- 成员 D：版本链正确性验证（前后 tx 连接）。
- 成员 E：性能含义说明（查最新版 O(1)）。

一键验证命令：
`cd backend; pytest tests/test_records.py -v`

### 第 3 次：Fabric 历史查询与时间线

已实现内容范围：
- `GetHistoryForKey` 历史查询链码方法。
- 后端 chain-history 接口与权限控制。
- 网关历史缓存命中机制。

5 人分工：
- 成员 A：链码历史查询实现。
- 成员 B：后端 history 与 chain-history 设计。
- 成员 C：缓存命中与失效逻辑。
- 成员 D：前端时间线展示与字段解释。
- 成员 E：`test_history.py` 数据结果与指标。

一键验证命令：
`cd backend; pytest tests/test_history.py -v`

### 第 4 次：文件哈希上链与链下加密

已实现内容范围：
- 文件上传、下载、校验接口。
- AES-256-GCM 加密存储、SHA-256 存证。
- Range 下载与完整性校验。

5 人分工：
- 成员 A：加密与哈希双层校验原理。
- 成员 B：后端文件接口（upload/download/verify/range）。
- 成员 C：前端文件上传下载与完整性标记。
- 成员 D：篡改攻击实验（密文篡改、哈希不一致）。
- 成员 E：性能与正确性测试数据汇总。

一键验证命令：
`cd backend; pytest tests/test_crypto.py tests/test_files.py -v`

### 第 5 次：ABAC 链上访问控制

已实现内容范围：
- Approve/Revoke/AccessRecord 链码控制。
- 过期、次数、撤销、MSP 绑定检查。
- 下载时消费授权与剩余次数回传。

5 人分工：
- 成员 A：ABAC 规则模型与状态机。
- 成员 B：链码 ABAC 方法实现细节。
- 成员 C：后端授权审批与下载消费逻辑。
- 成员 D：前端授权审批与撤销页面。
- 成员 E：对抗测试（绕过后端/MSP 冒用/耗尽/过期）。

一键验证命令：
`cd backend; pytest tests/test_abac.py -v`

### 第 6 次：链码事件与实时审计

已实现内容范围：
- 链码事件触发点。
- 后端事件总线、审计落库。
- WebSocket 实时通知与前端铃铛。

5 人分工：
- 成员 A：链上事件到链下通知的总架构。
- 成员 B：链码事件类型与触发时机。
- 成员 C：网关 listener 和 offset 机制。
- 成员 D：后端 WebSocket 与审计表。
- 成员 E：前端通知体验与延迟测试结果。

一键验证命令：
`cd backend; pytest tests/test_events.py -v`

### 第 7 次：CouchDB 富查询与分页

已实现内容范围：
- 3 类富查询链码方法。
- CouchDB 索引文件。
- 后端链上查询接口与分页。

5 人分工：
- 成员 A：链码 selector/use_index/pagination 实现。
- 成员 B：索引文件设计与字段选择。
- 成员 C：后端查询接口和角色权限。
- 成员 D：1000 条分页无丢失无重复实验。
- 成员 E：前端或接口联调展示与结果解读。

一键验证命令：
`cd backend; pytest tests/test_rich_query.py -v`

### 第 8 次：运维能力与收官验收

已实现内容范围：
- `/metrics`、`/health/live`、`/health/ready`。
- 限流、部署文件、监控配置、压测脚本。
- 全链路冒烟测试。

5 人分工：
- 成员 A：运维探针与指标体系。
- 成员 B：限流与安全扫描脚本。
- 成员 C：部署文件（compose.prod、nginx、备份）。
- 成员 D：Caliper/Locust 脚本与压测流程说明。
- 成员 E：收官冒烟测试与最终结论。

一键验证命令：
`cd backend; pytest tests/test_ops.py tests/test_smoke_e2e.py -v`

## 4. 最终一键总验收（可选）

- 后端全量：`cd backend; pytest tests -v`
- 链码全量：`cd ..\fabric-network\chaincode\medshare\javascript; npm test`

## 5. 写作约束（避免写到未实现内容）

- 只引用仓库现有文件和测试结果。
- 没有实际跑出的 TPS/P95 数字，不写具体数值，只写"脚本与配置已完成"。
- 每个小节至少放一个证据：代码路径、接口响应或测试输出。