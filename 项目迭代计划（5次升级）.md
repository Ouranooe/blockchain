# MedShare 五次技术升级计划（迭代 9-13）

> 上接 [项目迭代计划（8次）.md](项目迭代计划（8次）.md) 八次基础迭代收官。
> 本计划在 **MedShare 已有联盟链架构** 上继续深化 **区块链技术** 主题，每次产出"代码 + mocha + pytest + 实验报告"。

## 当前基线（迭代 1-8 已完成）

- 链码已实现 14 个方法（病历版本链、历史查询、链上 ABAC、链码事件、CouchDB 富查询）
- 链码 Mocha 测试 49 条；后端 pytest 107 条
- 生产化部署、Caliper 压测、安全扫描全部就绪

链码核心键空间：
- `RECORD_LATEST_{id}` / `RECORD_{id}_v{version}` — 病历最新版与各版本
- `REQ_{id}` — 访问申请

## 五次升级总览

| # | 主题 | 区块链核心技术点 | 主要交付 |
|---|------|----------------|----------|
| 9 | Merkle 批量锚定 + 包含证明 | Merkle 树、聚合存证 | `AnchorBatch` / `VerifyInclusion` 链码方法 + 后端聚合定时任务 |
| 10 | 链上多签治理 | 链上多签、双 MSP endorse、状态机 | `GovernanceAction` 链码族 + 后端治理 API + admin 治理页 |
| 11 | 链上紧急冻结 | 链上不可绕过、合约组合 | `FreezeRecord` / `UnfreezeRecord`（解冻需引用治理 TxID） |
| 12 | 链码 v2 升级 + 状态迁移 | 链码生命周期、Schema 演进 | record.category / request.purpose 字段 + 迁移方法 + 兼容读 |
| 13 | 数据共享积分 (FT) | 原生 FT 账本、链上经济激励 | `Credit*` 链码族 + 积分获取规则 + 前端余额展示 |

每次迭代独立可上线、独立可回滚。

---

## 迭代 9：Merkle 批量锚定 + 包含证明

### 9.1 目标
把"每条病历一笔上链交易"改为"**每日批量聚合一次锚定**"，链上只存一个 Merkle 根；个别证明可由链下重建并通过链码验证。

### 9.2 区块链技术点
- **Merkle 树**：标准二叉树 + SHA-256，奇数补自身（Bitcoin 风格）
- **包含证明 (Inclusion Proof)**：兄弟节点路径 + 位标记
- **聚合存证**：N 条单上链 → 1 条锚定 + N 个链下证明

### 9.3 改动清单
- 链码新增方法（在 [medshare-contract.js](fabric-network/chaincode/medshare/javascript/lib/medshare-contract.js)）：
  - `AnchorRecordBatch(batchId, merkleRoot, leafCount, createdAt)` — 写入 `BATCH_{batchId}`
  - `GetAnchorBatch(batchId)` — 读取
  - `VerifyRecordInclusion(batchId, leafHash, proofJson)` — 链上验证证明
  - `ListAnchorBatches(pageSize, bookmark)` — 富查询所有锚定（CouchDB 索引）
- 后端新增模块 `backend/app/merkle.py`：
  - `compute_merkle_root(leaves)` / `compute_proof(leaves, index)` / `verify_proof(root, leaf, proof)`
  - `MerkleAnchorBatch` 表（id / merkle_root / leaf_count / record_id_low / record_id_high / tx_id / created_at）
- 后端新增 API：
  - `POST /api/anchor/run`（admin）— 把"自上次锚定后新增的所有病历"聚合
  - `GET /api/records/{id}/proof` — 返回该病历的 Merkle 证明 + 所在批次的链上 TxID
  - `POST /api/anchor/verify` — 在链上验证一份证明
- 前端：admin 审计页增加"批量锚定"按钮 + 病历详情显示"包含证明 ✓ / Anchored in Batch #N"

### 9.4 测试
- **mocha**（≥6 条）：
  - 锚定一次 → `GetAnchorBatch` 取回字段一致
  - 单叶子 Merkle 根 == 叶子哈希
  - 两叶子 / 三叶子（奇数补） / 八叶子 验证证明
  - 篡改叶子哈希 → 验证失败
  - 错误 batchId → 抛错
- **pytest**（≥6 条）：
  - 创建 5 份病历 → 锚定 → 5 笔 proof 都能验证通过
  - 篡改一份 proof bit → API 返回 false
  - 第二次锚定时跳过已锚定病历
  - 普通用户 403、admin 200

### 9.5 量化指标
- 链上交易数下降：N 条单上链 → 1 条锚定，节省 ~N×
- 单批 100 笔 proof 生成 < 100ms（pytest 测吞吐）

---

## 迭代 10：链上多签治理（双 MSP 联合签发）

### 10.1 目标
让"高风险动作（批量撤销 / 强制下架记录 / 紧急冻结）"不能由单一组织决定 —— 需要 **Org1MSP + Org2MSP 双方在链上 endorse** 才能 EXECUTE。

### 10.2 区块链技术点
- **多签（M-of-N approval）**：链上状态机收集签名
- **MSP 身份验证**：`ctx.clientIdentity.getMSPID()` 强约束
- **组合合约**：迭代 11 解冻会引用治理 TxID

### 10.3 改动清单
- 链码新增（同一合约新增 namespace）：
  - `ProposeGovernanceAction(actionId, kind, payloadJson, proposedAt)` — kind ∈ `{FREEZE_RECORD, UNFREEZE_RECORD, BATCH_REVOKE}`
  - `ApproveGovernanceAction(actionId, approvedAt)` — 记录 `approverMsp = caller MSP`
  - `ExecuteGovernanceAction(actionId, executedAt)` — 校验已有 ≥2 个不同 MSP 批准 → 状态机置 EXECUTED
  - `GetGovernanceAction(actionId)` / `ListGovernanceActions(status?, pageSize, bookmark)`
  - 状态机：PROPOSED → APPROVED（满足后） → EXECUTED → DONE；任何阶段可 REJECTED
- 后端：
  - 表 `governance_actions`（mysql 镜像）
  - API：`POST /api/governance/actions`、`POST /api/governance/actions/{id}/approve`、`POST /api/governance/actions/{id}/execute`、`GET /api/governance/actions`
- 前端 admin 页：治理审批面板，区分待批准 / 待执行 / 已完成 / 已拒绝

### 10.4 测试（mocha + pytest 各 ≥6 条）
- 提案 → 单签 → 不能执行
- 同 MSP 两次签 → 不算双签（去重）
- Org1MSP + Org2MSP 双签 → 可 EXECUTE
- 非 admin 角色提案 / 签批 → 拒绝（迭代 1 加入的 `msp_org` + role 校验）
- 状态机非法跃迁全部拒绝
- 事件 `GovernanceProposed/Approved/Executed` 正确发射

### 10.5 量化指标
- 单签率 0%、双签后执行成功率 100%
- 提案到执行端到端 ≤ 3 笔上链交易

---

## 迭代 11：链上紧急冻结 + 治理解冻闭环

### 11.1 目标
患者发现自己病历被滥用时，可"**一键紧急冻结**"链上状态；冻结后 `UpdateMedicalRecordEvidence` / `AccessRecord` 都被链码层拒绝。解冻必须**通过迭代 10 的治理流程**才能完成 —— 体现"区块链链上不可绕过 + 治理闭环"。

### 11.2 区块链技术点
- 链码字段守卫（`frozen` 标志）所有写动作前检查
- 链码组合：解冻引用治理 ExecutedTxID
- 链上不可绕过：即使后端误操作也会被链码拒

### 11.3 改动清单
- 链码：
  - record 添加 `frozen`/`frozenAt`/`freezeTxId`/`unfreezeTxId` 字段
  - 新方法 `FreezeRecord(recordId, requesterId, reasonHash, frozenAt)` — 仅 `patientId == requesterId` 通过
  - 新方法 `UnfreezeRecord(recordId, governanceActionId, unfrozenAt)` — 校验治理动作为 EXECUTED 且 `kind == UNFREEZE_RECORD` 且 `payload.recordId == recordId`
  - 在 `UpdateMedicalRecordEvidence`、`AccessRecord` 中加 `if (record.frozen) throw new Error(...)`
- 后端：
  - 表 `medical_records` 增 `frozen` / `frozen_at` / `freeze_tx_id` / `unfreeze_tx_id` 列
  - API：`POST /api/records/{id}/freeze`（患者）、`POST /api/records/{id}/unfreeze`（admin，需 governance action id）
- 前端患者侧：每条自己病历右侧增"紧急冻结"红色按钮 + 治理走向 admin

### 11.4 测试（mocha + pytest 各 ≥6 条）
- 患者冻结后再修订 → 链码拒绝（关键）
- 患者冻结后再读 (AccessRecord) → 链码拒绝
- 非归属患者无法冻结
- 解冻无治理 TxID → 拒绝
- 解冻治理 TxID 但 kind 不匹配 → 拒绝
- 解冻成功后写入 / 读取恢复

### 11.5 量化指标
- 冻结后所有写动作拒绝率 100%
- 解冻链上引用追溯率 100%（每次解冻可追到 governance action）

---

## 迭代 12：链码 v2 升级 + 状态迁移 + 兼容读

### 12.1 目标
演示 **Fabric 链码生命周期升级**：在不丢失老数据的前提下，给 record / request 加业务字段，并提供老数据兼容读 + 一次性迁移方法。

### 12.2 区块链技术点
- 链码版本管理（contract `Init()` 写入 schemaVersion）
- 老数据兼容读：`_normalize()` 给缺失字段填默认值
- 显式迁移：`MigrateV2(batchKeys[])` 一次性把老数据写回新结构（产生 history 痕迹）

### 12.3 改动清单
- 链码：
  - record 新增 `category` ∈ `{GENERAL, INPATIENT, OUTPATIENT, EMERGENCY}`，默认 `GENERAL`
  - request 新增 `purpose` ∈ `{TREATMENT, RESEARCH, AUDIT}`，默认 `TREATMENT`
  - `_normalizeRecord(obj)` / `_normalizeRequest(obj)` 在所有读路径调用
  - 新方法 `MigrateRecordsV2(batchJson)`、`MigrateRequestsV2(batchJson)`（admin only）
  - 新方法 `GetSchemaVersion()` 返回 `v2`
  - CouchDB 新增索引：`indexCategory`、`indexPurpose`
- 后端：
  - 表加 `category` / `purpose` 列
  - 创建病历 / 申请的 API 支持新字段
  - `/api/admin/migrate/records-v2` admin 触发链上迁移
- 前端：
  - 上传 / 申请表单增加下拉选择
  - 病历详情显示 category；申请列表显示 purpose 角标

### 12.4 测试（mocha + pytest 各 ≥6 条）
- 老 record (无 category) 读出来 category="GENERAL"
- 新 record 写入 category 持久化
- 富查询按 category 过滤
- 迁移后 record value 含 `_migratedAt`
- 非 admin MSP 调用 Migrate → 拒
- `GetSchemaVersion()` 返回 "v2"

### 12.5 量化指标
- 老数据 100% 可读、迁移失败率 0%
- 迁移耗时 < O(n) 100 条 < 1s

---

## 迭代 13：数据共享积分（FT）+ 经济激励闭环

### 13.1 目标
在链上引入 **简单同质化代币 (FT)** 作为"共享积分"，与业务自动联动：
- 医院上传病历 +5 分
- 医院被访问（`AccessRecord` 触发）→ 上传医院 +1 分
- 患者审批 → +1 分
积分可链上转账（用户 → 用户），构建初步的数据经济闭环。

### 13.2 区块链技术点
- 链上 FT 账本（账户 → 余额）
- 转账原子性（`putState` 在同一交易里完成扣加）
- 防双花（余额校验 + 链码 ABAC）
- 事件 `CreditCredited` / `CreditDebited` / `CreditTransferred`

### 13.3 改动清单
- 链码：
  - 键 `CREDIT_{userId}` 存 `{userId, balance, updatedAt}`
  - 方法：
    - `CreditMint(toUserId, amount, reasonCode, mintedAt)` — admin only
    - `CreditTransfer(fromUserId, toUserId, amount, reasonCode, txAt)` — 余额校验 + 双方 putState
    - `CreditBalance(userId)`、`CreditHistory(userId, pageSize, bookmark)`（CouchDB 富查询）
  - `CreateMedicalRecordEvidence` 完成后自动给 uploader +5 分（内部调用 `_creditMint`）
  - `AccessRecord` 成功后给上传医院 +1 分
  - `ApproveAccessRequest` 后给患者 +1 分
- 后端：
  - 表 `credit_history`（id / user_id / delta / reason_code / tx_id / created_at）
  - API：`GET /api/credits/balance`、`GET /api/credits/history`、`POST /api/credits/transfer`
- 前端：右上角全角色显示"积分：N"，点开看流水

### 13.4 测试（mocha + pytest 各 ≥7 条）
- 上传病历后 uploader 余额 +5
- AccessRecord 后 uploader 余额 +1
- 双方转账原子性（fromBalance 不足 → 抛错且 toBalance 不变）
- 余额 = 历史正负累加
- 非 admin Mint → 拒
- 不存在用户 transfer → 拒
- 富查询历史按时间段过滤

### 13.5 量化指标
- 转账原子性 100% 通过（pytest 多线程 sanity）
- 积分账户与 history 累加一致性 100%

---

## 每次迭代的统一执行步骤

1. **更新文档**：`docs/iteration-{N}.md`（覆盖目标 → 设计 → 实现 → 测试 → 数据 → 反思）
2. **代码改动**：链码 + 后端 + 前端 + 测试
3. **跑测**：
   ```bash
   cd backend && pytest tests/ -v
   cd fabric-network/chaincode/medshare/javascript && npm test
   ```
4. **自查/审阅**：清单见每个 iteration 文档"Done Definition"
5. **提交 + 推送**：commit 信息约定 `迭代 N：<主题>`
6. **进入下一迭代**

---

## 风险与备选

| 风险 | 缓解 |
|---|---|
| 状态机复杂度上升 | 每次迭代独立写一张状态机表格，pytest 显式断言所有非法跃迁 |
| 链码膨胀 | 按 namespace 内部归类（governance / credit / merkle），便于未来拆分成独立链码 |
| 老数据兼容 | 迭代 12 集中提供 `_normalize*()` 工具；迭代 9/11/13 同样在 read 路径降级处理缺省字段 |
| 真链验证缺失 | 沿用迭代 1-8 的 mocha + mock CouchDB 策略；端到端真链验证留给 WSL2 + Caliper 复用 |

---

## 总结

- 迭代 9-11：**数据完整性 / 治理 / 不可绕过** 三件套，深化 Fabric 链码的密码学价值
- 迭代 12：**链码生命周期** —— 工程上的"如何活下去"
- 迭代 13：**链上经济** —— 把"数据共享"变成"双向激励"

五次升级累计可覆盖：**Merkle 聚合存证、链上多签治理、链上紧急冻结、链码升级与状态迁移、链上 FT 账本**。
完整覆盖 Fabric 区块链从"哈希聚合 → 链上治理 → 合约组合 → 升级演进 → 链上资产"五个进阶主题。
